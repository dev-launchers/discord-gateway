---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: discord-gateway
  namespace: discord-gateway
spec:
  template:
    spec:
      $setElementOrder/containers:
      - name: discord-gateway
      containers:
      - image: devlaunchers/discord-gateway:flux-write-check-5b16c4fa26
        name: discord-gateway
